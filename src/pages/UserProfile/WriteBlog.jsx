function WriteBlog() {
    // will be used for user to create a new blog and will show existing blogs of user
    // alongside with a "create article" button

    // existing blogs of logged in user will be showed with a carousel the bottom part of this page (blogs file might be used)
    //which will have options (onclick on the blog card)
    //optionscould be;
    // changing the blog details with a save button
    // deleting the existing blogs with a delete button
    return <div></div>
}

export default WriteBlog;
function UserDetails() {
    //will be used as showing (any) another user's general details to (any) user
    //user will be reach here via onclick to (any) user's profile photo showed in the blogs' bottom part. 
    //this details may contain;
    //(related user's) Name Surname
    //(related user's) Biography
    //(related user's) Location
    //(related user's) a set of previous blogs with a carousel (blogs file might be used)
    return <div></div>
}

export default UserDetails;